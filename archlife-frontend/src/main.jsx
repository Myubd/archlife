import React from "react";
import ReactDOM from "react-dom/client";
import ArchLifeApp from "./ArchLifeApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ArchLifeApp />
  </React.StrictMode>
);
