# Neon等のPostgres接続文字列。本番(Render)ではダッシュボードの環境変数として設定する。
# ローカルのdocker-compose利用時はdocker-compose.yml側で自動設定されるため空のままでよい。
DATABASE_URL=

# 外部API(Claude/GPT)を使う場合のみ設定する。
# 空のままなら、AIはローカルLLM(Qwen)のみで動作する。
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
